Resources:
  BarbershopCDNOAC:
    Type: AWS::CloudFront::OriginAccessControl
    Properties:
      OriginAccessControlConfig:
        Name: BarbershopBucketCDNOAC
        Description: OAC For BarbershopBucket
        OriginAccessControlOriginType: s3
        SigningBehavior: always
        SigningProtocol: sigv4

  BarbershopBucketCDN:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        HttpVersion: http2and3
        Origins:
          - Id: S3Origin
            S3OriginConfig: {}
            DomainName: !GetAtt BarbershopBucket.RegionalDomainName
            OriginAccessControlId: !Ref BarbershopCDNOAC
        DefaultCacheBehavior:
          Compress: true
          ViewerProtocolPolicy: redirect-to-https
          TargetOriginId: S3Origin
          AllowedMethods:
            - GET
            - HEAD
          CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6 #aws-managed-cache-police
        ViewerCertificate:
          CloudFrontDefaultCertificate: true

  BarbershopBucketCDNPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref BarbershopBucket
      PolicyDocument:
        Version: "2008-10-17"
        Id: PolicyForCloudFrontServicePrincipal
        Statement:
          - Sid: AllowCloudFrontServicePrincipal
            Effect: Allow
            Principal:
              Service: cloudfront.amazonaws.com
            Action:
              - s3:GetObject
            Resource: !Sub "${BarbershopBucket.Arn}/*"
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub "arn:aws:cloudfront::${AWS::AccountId}:distribution/${BarbershopBucketCDN}" #ver antes do deploy
